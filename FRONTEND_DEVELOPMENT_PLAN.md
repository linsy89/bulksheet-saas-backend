# Bulksheet SaaS - 前端开发计划

**版本**: v1.0
**创建日期**: 2025-11-07
**项目状态**: 后端 Stage 1-4 已完成，前端待开发
**目标**: 开发完整的 Web 前端界面，支持 Stage 1-4 全流程操作

---

## 目录

- [1. 项目概述](#1-项目概述)
- [2. 技术选型](#2-技术选型)
- [3. 功能规划](#3-功能规划)
- [4. 页面设计](#4-页面设计)
- [5. 组件架构](#5-组件架构)
- [6. 状态管理](#6-状态管理)
- [7. API 集成](#7-api-集成)
- [8. 开发计划](#8-开发计划)
- [9. 部署方案](#9-部署方案)

---

## 1. 项目概述

### 1.1 项目背景

Bulksheet SaaS 是一个 AI 驱动的亚马逊广告关键词生成工具，帮助广告投放人员快速生成高质量的广告关键词组合。后端已完成 4 个核心功能的开发：
- **属性词生成**: AI 智能扩展产品属性词（如"cute"可扩展为"adorable"、"kawaii"等）
- **属性词筛选**: 人工审核和筛选AI生成的属性词
- **本体词扩展**: 扩展产品核心词的变体（如"phone case"扩展为"phone cover"等）
- **广告表导出**: 输入产品信息和预算，导出符合亚马逊规范的Bulksheet文件

前端需要为以上4个功能提供完整的用户界面。

### 1.2 核心价值

- **向导式流程**: 引导用户完成从属性词生成到广告表导出的全流程操作
- **AI 自动化**: 减少手动输入，AI 自动扩展关键词，提升10倍工作效率
- **数据可视化**: 实时展示词汇统计、推荐度、搜索价值等关键指标
- **响应式设计**: 支持桌面和平板设备，随时随地办公

### 1.3 用户角色

**主要用户**: 亚马逊广告投放人员
- 需要快速生成大量关键词
- 熟悉亚马逊广告规范
- 追求效率和准确性

---

## 2. 技术选型

### 2.1 核心技术栈

| 技术 | 版本 | 用途 | 选择理由 |
|------|------|------|---------|
| **React** | 18.x | UI 框架 | 生态成熟，组件化开发 |
| **TypeScript** | 5.x | 类型系统 | 提升代码质量和可维护性 |
| **Vite** | 5.x | 构建工具 | 快速开发，HMR 体验好 |
| **React Router** | 6.x | 路由管理 | 官方推荐，功能完善 |
| **TanStack Query** | 5.x | 数据获取 | 缓存、重试、自动刷新 |
| **Zustand** | 4.x | 状态管理 | 轻量级，API 简洁 |
| **Ant Design** | 5.x | UI 组件库 | 企业级组件，开箱即用 |
| **Tailwind CSS** | 3.x | 样式方案 | 快速开发，响应式友好 |
| **Axios** | 1.x | HTTP 客户端 | 拦截器，错误处理完善 |

### 2.2 技术栈对比

#### 为什么选择 Ant Design？

| 对比项 | Ant Design | Material-UI | Chakra UI |
|-------|-----------|-------------|-----------|
| 组件丰富度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| 企业级场景 | ✅ 专为后台设计 | ⚠️ 偏消费级 | ⚠️ 通用 |
| 中文文档 | ✅ 完善 | ⚠️ 一般 | ❌ 较少 |
| Table 组件 | ✅ 功能强大 | ⚠️ 需扩展 | ❌ 较弱 |
| Form 表单 | ✅ 完整验证体系 | ✅ 完善 | ⚠️ 基础 |

**结论**: Ant Design 更适合本项目的后台管理场景。

#### 为什么选择 Zustand 而非 Redux？

| 特性 | Zustand | Redux Toolkit | Jotai |
|------|---------|---------------|-------|
| 学习曲线 | ⭐ 简单 | ⭐⭐⭐ 陡峭 | ⭐⭐ 中等 |
| 代码量 | ⭐⭐⭐⭐⭐ 最少 | ⭐⭐ 较多 | ⭐⭐⭐⭐ 少 |
| 性能 | ⭐⭐⭐⭐⭐ 优秀 | ⭐⭐⭐⭐ 良好 | ⭐⭐⭐⭐⭐ 优秀 |
| TypeScript | ✅ 原生支持 | ✅ 完善 | ✅ 完善 |
| DevTools | ✅ 支持 | ✅ 官方工具 | ❌ 无 |

**结论**: 本项目状态管理需求简单，Zustand 代码量少且性能优秀。

### 2.3 开发工具

- **VS Code**: 代码编辑器
- **ESLint + Prettier**: 代码规范
- **Husky**: Git Hooks
- **pnpm**: 包管理器（比 npm 快 2-3 倍）

---

## 3. 功能规划

### 3.1 功能模块总览

```
┌──────────────────────────────────────────────────────────┐
│              Bulksheet SaaS - 业务流程图                │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │  步骤1      │→ │  步骤2      │→ │  步骤3      │    │
│  │  属性词生成  │  │  属性词筛选  │  │  本体词扩展  │    │
│  │  (AI扩展)   │  │  (人工审核)  │  │  + 搜索词   │    │
│  └─────────────┘  └─────────────┘  └─────────────┘    │
│                                          ↓              │
│                                   ┌─────────────┐      │
│                                   │  步骤4      │      │
│                                   │  广告表导出  │      │
│                                   │  (Excel文件)│      │
│                                   └─────────────┘      │
└──────────────────────────────────────────────────────────┘
```

### 3.2 核心页面列表

| 页面名称 | 路由 | 业务功能描述 | 优先级 |
|------|------|------|--------|
| 首页 | `/` | 工具介绍 + 开始使用按钮 | P1 |
| 属性词生成 | `/attributes/generate` | 输入产品属性概念，AI 自动生成扩展属性词 | P0 |
| 属性词筛选 | `/attributes/select/:taskId` | 查看AI生成的属性词，人工筛选需要的词汇 | P0 |
| 本体词扩展 | `/entity-words/:taskId` | 扩展产品核心词变体，生成更多搜索覆盖 | P0 |
| 搜索词管理 | `/search-terms/:taskId` | 查看属性词+本体词组合结果，管理最终搜索词 | P0 |
| 广告表导出 | `/export/:taskId` | 输入产品SKU/ASIN和预算，导出Excel广告表 | P0 |
| 任务列表 | `/tasks` | 查看历史任务记录（未来功能） | P2 |

### 3.3 功能清单

#### 页面1: 属性词生成

**业务目标**: 用户输入产品属性概念（如"cute"），AI 自动扩展出相关属性词（如"adorable"、"kawaii"等）

**功能点**:
- [x] 后端 API: ✅ 已完成
- [ ] 输入表单: 输入产品属性概念（必填）和产品核心词（选填）
- [ ] AI 生成: 显示"AI 正在生成中..."加载状态
- [ ] 结果展示: 表格显示生成的属性词及其中文翻译、使用场景
- [ ] 统计看板: 显示总词数、原词/同义词/变体词分布
- [ ] 下一步: "进入筛选"按钮

#### 页面2: 属性词筛选

**业务目标**: 人工审核 AI 生成的属性词，选择需要的词汇，排除不合适的词

**功能点**:
- [x] 后端 API: ✅ 已完成
- [ ] 属性词表格: 可勾选，显示词汇、类型、搜索价值星级、推荐度
- [ ] 快速筛选: 按推荐度、搜索价值、词汇类型过滤
- [ ] 添加自定义词: 手动添加AI未生成的属性词
- [ ] 删除词汇: 删除不需要的词
- [ ] 保存选择: 提交选中的词汇到系统
- [ ] 下一步: "进入本体词扩展"按钮

#### 页面3: 本体词扩展

**业务目标**: 扩展产品核心词的变体（如"phone case"扩展为"phone cover"、"mobile case"等），覆盖更多用户搜索习惯

**功能点**:
- [x] 后端 API: ✅ 已完成
- [ ] AI 生成按钮: 点击"生成本体词变体"调用 AI
- [ ] 本体词列表: 显示原始本体词及其变体
- [ ] 勾选筛选: 选择需要的本体词变体
- [ ] 添加自定义: 手动添加本体词变体
- [ ] 保存选择: 提交选中的本体词
- [ ] 下一步: "生成搜索词"按钮

#### 页面4: 搜索词管理

**业务目标**: 查看"属性词 + 本体词"的组合结果（如"cute phone case"），管理最终的搜索词列表

**功能点**:
- [x] 后端 API: ✅ 已完成
- [ ] 搜索词表格: 显示完整搜索词、来源属性词、来源本体词、词长度
- [ ] 分页: 支持大量搜索词的分页查看
- [ ] 筛选: 按属性词或本体词筛选搜索词
- [ ] 批量删除: 勾选并删除不需要的搜索词
- [ ] 下一步: "导出广告表"按钮

#### 页面5: 广告表导出

**业务目标**: 输入产品信息（SKU/ASIN/型号）和广告预算，导出符合亚马逊规范的 Bulksheet Excel 文件

**功能点**:
- [x] 后端 API: ✅ 已完成
- [ ] 产品信息表单: SKU输入框、ASIN输入框、手机型号下拉选择
- [ ] 预算信息表单: 每日预算、广告组默认出价、关键词出价
- [ ] 预览信息: 实时显示将生成的广告活动名称、广告组名称
- [ ] 导出按钮: 点击下载 Excel 文件
- [ ] 成功提示: "文件已成功下载"提示信息

---

## 4. 页面设计

### 4.1 设计原则

1. **渐进式引导**: 清晰的步骤指示器（Breadcrumb）
2. **即时反馈**: Loading 状态、成功/失败提示
3. **数据可视化**: 统计卡片、进度条
4. **容错设计**: 表单验证、错误提示

### 4.2 布局结构

#### 全局布局

```
┌─────────────────────────────────────────────────────┐
│  Header                                              │
│  [Logo] Bulksheet SaaS    [任务列表] [帮助] [设置]  │
├─────────────────────────────────────────────────────┤
│  Breadcrumb（进度导航）                              │
│  首页 > 属性词生成 > 属性词筛选 > 本体词扩展 > ...   │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Main Content Area                                   │
│                                                      │
│  (各功能页面内容)                                     │
│                                                      │
│                                                      │
├─────────────────────────────────────────────────────┤
│  Footer                                              │
│  © 2025 Bulksheet SaaS | Powered by AI               │
└─────────────────────────────────────────────────────┘
```

#### 页面1: 属性词生成

```
┌─────────────────────────────────────────────────────┐
│  步骤1：属性词生成（AI 扩展）                         │
├─────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────────────────────────────────────────┐  │
│  │  输入区域                                     │  │
│  │                                               │  │
│  │  产品属性概念:  [________] (必填)             │  │
│  │  示例：cute / waterproof / durable            │  │
│  │                                               │  │
│  │  产品核心词:    [phone case] (可选)           │  │
│  │  示例：phone case / laptop bag                │  │
│  │                                               │  │
│  │  [AI 生成属性词] 按钮                         │  │
│  └──────────────────────────────────────────────┘  │
│                                                      │
│  ┌──────────────────────────────────────────────┐  │
│  │  AI 生成结果                                  │  │
│  │                                               │  │
│  │  数据统计看板:                                │  │
│  │  ┌────────┐ ┌────────┐ ┌────────┐          │  │
│  │  │   15   │ │   1    │ │   3    │          │  │
│  │  │ 总词数 │ │ 原始词 │ │ 同义词 │          │  │
│  │  └────────┘ └────────┘ └────────┘          │  │
│  │  ┌────────┐ ┌────────┐                     │  │
│  │  │   6    │ │   5    │                     │  │
│  │  │ 变体词 │ │ 高价值 │                     │  │
│  │  └────────┘ └────────┘                     │  │
│  │                                               │  │
│  │  属性词列表:                                  │  │
│  │  ┌─────────────────────────────────────────┐│  │
│  │  │ 属性词 | 中文 | 搜索价值 | 推荐 | 说明 ││  │
│  │  ├─────────────────────────────────────────┤│  │
│  │  │ cute | 可爱 | ⭐⭐⭐⭐⭐ | ✅ | 高搜索 ││  │
│  │  │ adorable | 讨喜 | ⭐⭐⭐⭐ | ✅ | ... ││  │
│  │  │ ...                                     ││  │
│  │  └─────────────────────────────────────────┘│  │
│  │                                               │  │
│  │  [下一步：进入筛选 →] 按钮                    │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

#### 页面2: 属性词筛选

```
┌─────────────────────────────────────────────────────┐
│  步骤2：属性词筛选（人工审核）                        │
│  任务信息: 概念=cute | 产品=phone case | 共15个词汇  │
├─────────────────────────────────────────────────────┤
│                                                      │
│  操作工具栏:                                         │
│  [快速筛选▼] [只看推荐] [添加自定义词]    已选: 8/15│
│                                                      │
│  属性词表格（可勾选）:                               │
│  ┌────────────────────────────────────────────────┐ │
│  │ 选中 | 属性词 | 类型 | 搜索价值 | 推荐 | 操作 │ │
│  ├────────────────────────────────────────────────┤ │
│  │ ☑ | cute | 原词 | ⭐⭐⭐⭐⭐ | ✅ | [删除] │ │
│  │ ☑ | adorable | 同义词 | ⭐⭐⭐⭐ | ✅ | [删] │ │
│  │ ☐ | kawaii | 变体 | ⭐⭐⭐ | ⚠️ | [删除]   │ │
│  │ ...                                            │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  操作按钮:                                           │
│  [← 返回重新生成] [保存并继续 →]                     │
└─────────────────────────────────────────────────────┘
```

#### 页面3: 本体词扩展

```
┌─────────────────────────────────────────────────────┐
│  步骤3：本体词扩展（覆盖更多搜索习惯）                │
│  原始产品核心词: phone case                          │
├─────────────────────────────────────────────────────┤
│                                                      │
│  💡 提示:                                            │
│  "扩展本体词变体可以覆盖用户的不同搜索习惯，         │
│   如'phone case' → 'phone cover'、'mobile case'等"  │
│                                                      │
│  [AI 生成本体词变体] 按钮 (如果未生成)               │
│                                                      │
│  ─────── 或 已生成后显示 ───────                    │
│                                                      │
│  操作工具栏:                                         │
│  [添加自定义变体]  [批量选择]    已选: 6/12         │
│                                                      │
│  本体词变体列表:                                     │
│  ┌────────────────────────────────────────────────┐ │
│  │ 选中 | 本体词变体 | 类型 | 搜索价值 | 操作 |   │ │
│  ├────────────────────────────────────────────────┤ │
│  │ ☑ | phone case | 原词 | ⭐⭐⭐⭐⭐ | [删]   │ │
│  │ ☑ | phone cover | 同义词 | ⭐⭐⭐⭐⭐ | [删] │ │
│  │ ☑ | mobile case | 变体 | ⭐⭐⭐⭐ | [删除] │ │
│  │ ...                                            │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  操作按钮:                                           │
│  [← 返回] [保存并生成搜索词 →]                       │
└─────────────────────────────────────────────────────┘
```

#### 页面4: 搜索词管理

```
┌─────────────────────────────────────────────────────┐
│  步骤4：搜索词管理（最终关键词列表）                  │
│  ✅ 组合完成: 8属性词 × 6本体词 = 48个最终搜索词     │
├─────────────────────────────────────────────────────┤
│                                                      │
│  筛选工具栏:                                         │
│  按来源筛选: [属性词▼] [本体词▼]  [批量删除选中]    │
│                                                      │
│  搜索词列表（分页显示）:                             │
│  ┌────────────────────────────────────────────────┐ │
│  │ 选 | 搜索词组合 | 来源属性 | 来源本体 | 长度 |  │ │
│  ├────────────────────────────────────────────────┤ │
│  │ ☐ | cute phone case | cute | phone case | 15 │ │
│  │ ☐ | cute phone cover | cute | phone cover | 16││
│  │ ☐ | adorable phone case | adorable | ... | 19││
│  │ ...                                            │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  分页控制: [← 上一页] 第1/3页 [下一页 →]            │
│                                                      │
│  操作按钮:                                           │
│  [← 返回修改] [导出广告表 →]                         │
└─────────────────────────────────────────────────────┘
```

#### 页面5: 广告表导出

```
┌─────────────────────────────────────────────────────┐
│  步骤5：广告表导出（生成亚马逊Bulksheet）             │
│  📊 准备导出48个搜索词 + 7个否定词                   │
├─────────────────────────────────────────────────────┤
│                                                      │
│  📦 产品信息:                                        │
│  ┌────────────────────────────────────────────────┐ │
│  │  SKU (库存编号):  [___________________]        │ │
│  │  ASIN (商品编码): [__________] (10位字符)      │ │
│  │  手机型号:        [iPhone 16 Pro Max ▼]        │ │
│  │                                                │ │
│  │  [✓ 保存产品信息] 按钮                         │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  💰 广告预算设置:                                    │
│  ┌────────────────────────────────────────────────┐ │
│  │  每日预算:       $[____] (美元/天)             │ │
│  │  广告组默认出价: $[____] (美元/点击)           │ │
│  │  关键词出价:     $[____] (美元/点击)           │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  👁️ 预览信息:                                        │
│  ┌────────────────────────────────────────────────┐ │
│  │  ✓ 广告活动名称: XXX-SKU cute iPhone 16 Pro    │ │
│  │  ✓ 广告组名称:   cute iPhone 16 Pro Max        │ │
│  │  ✓ 关键词数量:   48个                           │ │
│  │  ✓ 否定关键词:   7个（排除词）                  │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  [← 返回修改] [📥 下载Excel文件] 按钮                │
└─────────────────────────────────────────────────────┘
```

### 4.3 组件设计规范

#### 颜色主题

```typescript
const theme = {
  primary: '#1890ff',      // 主色（Ant Design 默认蓝）
  success: '#52c41a',      // 成功绿
  warning: '#faad14',      // 警告橙
  error: '#f5222d',        // 错误红
  text: {
    primary: 'rgba(0, 0, 0, 0.85)',
    secondary: 'rgba(0, 0, 0, 0.65)',
    disabled: 'rgba(0, 0, 0, 0.25)',
  },
  background: {
    default: '#f0f2f5',
    paper: '#ffffff',
  },
}
```

#### 间距规范

```typescript
const spacing = {
  xs: '4px',
  sm: '8px',
  md: '16px',
  lg: '24px',
  xl: '32px',
}
```

---

## 5. 组件架构

### 5.1 组件层级

```
App
├── Layout（全局布局）
│   ├── Header（顶部导航）
│   │   ├── Logo
│   │   ├── Navigation（导航菜单）
│   │   └── UserMenu（用户菜单）
│   ├── Breadcrumb（面包屑导航）
│   ├── Content（内容区）
│   │   └── Router（路由）
│   │       ├── HomePage（首页）
│   │       ├── AttributeGeneratePage（属性词生成页）
│   │       ├── AttributeSelectPage（属性词筛选页）
│   │       ├── EntityWordPage（本体词扩展页）
│   │       ├── SearchTermPage（搜索词管理页）
│   │       └── ExportPage（广告表导出页）
│   └── Footer（页脚）
└── Providers（全局Provider）
    ├── QueryClientProvider（数据查询）
    └── StoreProvider（状态管理）
```

### 5.2 通用组件列表

#### 业务组件

| 组件名 | 业务功能描述 | 复用页面 |
|--------|------|--------|
| `AttributeTable` | 属性词表格（显示、筛选、选择属性词） | 属性词生成页、筛选页 |
| `EntityWordTable` | 本体词表格（显示、筛选、选择本体词变体） | 本体词扩展页 |
| `SearchTermTable` | 搜索词表格（显示、筛选搜索词组合） | 搜索词管理页 |
| `StatsCard` | 数据统计卡片（显示词汇数量、分布等） | 所有页面 |
| `LoadingOverlay` | AI 生成加载状态（显示生成进度） | 所有AI生成页面 |
| `WorkflowNavigation` | 流程导航按钮（上一步/下一步） | 所有页面 |

#### UI 组件

| 组件名 | 功能 | 基于 |
|--------|------|------|
| `PageHeader` | 页面标题 | Ant Design PageHeader |
| `DataTable` | 数据表格 | Ant Design Table |
| `FormInput` | 表单输入 | Ant Design Input/Select |
| `ConfirmModal` | 确认对话框 | Ant Design Modal |
| `Toast` | 提示消息 | Ant Design message |

### 5.3 核心组件实现

#### AttributeTable 组件

```typescript
interface AttributeTableProps {
  attributes: Attribute[];
  selectable?: boolean;           // 是否支持勾选
  selectedIds?: number[];         // 选中的ID列表
  onSelectionChange?: (ids: number[]) => void;
  onDelete?: (id: number) => void;
  loading?: boolean;
}

const AttributeTable: React.FC<AttributeTableProps> = ({
  attributes,
  selectable = false,
  selectedIds = [],
  onSelectionChange,
  onDelete,
  loading = false,
}) => {
  // 表格列定义
  const columns = [
    {
      title: '属性词',
      dataIndex: 'word',
      key: 'word',
      fixed: 'left',
    },
    {
      title: '类型',
      dataIndex: 'type',
      key: 'type',
      render: (type: string) => <Tag>{type}</Tag>,
    },
    {
      title: '搜索价值',
      dataIndex: 'search_value_stars',
      key: 'stars',
      render: (stars: number) => '⭐'.repeat(stars),
    },
    {
      title: '推荐',
      dataIndex: 'recommended',
      key: 'recommended',
      render: (recommended: boolean) => recommended ? '✅' : '⚠️',
    },
    {
      title: '说明',
      dataIndex: 'translation',
      key: 'translation',
      ellipsis: true,
    },
    {
      title: '操作',
      key: 'action',
      render: (_, record) => (
        <Button
          danger
          size="small"
          onClick={() => onDelete?.(record.id)}
        >
          删除
        </Button>
      ),
    },
  ];

  return (
    <Table
      columns={columns}
      dataSource={attributes}
      rowKey="id"
      loading={loading}
      rowSelection={selectable ? {
        selectedRowKeys: selectedIds,
        onChange: onSelectionChange,
      } : undefined}
      pagination={false}
    />
  );
};
```

---

## 6. 状态管理

### 6.1 Zustand Store 设计

#### 全局 Store（app-store.ts）

```typescript
interface AppStore {
  // 当前任务状态
  currentTaskId: string | null;
  currentStage: 1 | 2 | 3 | 4;

  // Actions
  setCurrentTask: (taskId: string) => void;
  setCurrentStage: (stage: number) => void;

  // 任务数据缓存（可选，也可用 TanStack Query）
  taskCache: Record<string, Task>;
  setTaskCache: (taskId: string, task: Task) => void;
}

export const useAppStore = create<AppStore>((set) => ({
  currentTaskId: null,
  currentStage: 1,
  taskCache: {},

  setCurrentTask: (taskId) => set({ currentTaskId: taskId }),
  setCurrentStage: (stage) => set({ currentStage: stage }),
  setTaskCache: (taskId, task) => set((state) => ({
    taskCache: { ...state.taskCache, [taskId]: task },
  })),
}));
```

#### Stage 2 Store（stage2-store.ts）

```typescript
interface Stage2Store {
  selectedIds: number[];
  newAttributes: NewAttribute[];
  deletedIds: number[];

  toggleSelection: (id: number) => void;
  selectAll: (ids: number[]) => void;
  clearSelection: () => void;
  addNewAttribute: (attr: NewAttribute) => void;
  removeNewAttribute: (index: number) => void;
  markAsDeleted: (id: number) => void;
  reset: () => void;
}
```

### 6.2 TanStack Query 使用

#### Queries（查询）

```typescript
// 查询任务详情
export const useTaskDetail = (taskId: string) => {
  return useQuery({
    queryKey: ['task', taskId],
    queryFn: () => api.getTaskDetail(taskId),
    enabled: !!taskId,
    staleTime: 5 * 60 * 1000, // 5分钟缓存
  });
};

// 查询搜索词列表（支持分页）
export const useSearchTerms = (
  taskId: string,
  page: number,
  pageSize: number
) => {
  return useQuery({
    queryKey: ['searchTerms', taskId, page, pageSize],
    queryFn: () => api.getSearchTerms(taskId, page, pageSize),
    enabled: !!taskId,
    keepPreviousData: true, // 分页时保留旧数据
  });
};
```

#### Mutations（变更）

```typescript
// 生成属性词
export const useGenerateAttributes = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: { concept: string; entity_word: string }) =>
      api.generateAttributes(data),
    onSuccess: (data) => {
      // 成功后跳转到 Stage 2
      navigate(`/stage2/${data.task_id}`);
      // 提示成功
      message.success(`生成了 ${data.metadata.total_count} 个属性词`);
    },
    onError: (error) => {
      message.error('生成失败，请重试');
    },
  });
};

// 更新属性词选择
export const useUpdateSelection = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: UpdateSelectionRequest) =>
      api.updateSelection(data.task_id, data),
    onSuccess: (data, variables) => {
      // 使缓存失效，触发重新获取
      queryClient.invalidateQueries(['task', variables.task_id]);
      message.success('保存成功');
    },
  });
};
```

---

## 7. API 集成

### 7.1 API 客户端封装

#### axios 配置（api/client.ts）

```typescript
import axios from 'axios';

const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000';

export const apiClient = axios.create({
  baseURL: API_BASE_URL,
  timeout: 60000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 请求拦截器
apiClient.interceptors.request.use(
  (config) => {
    // 可添加 token
    return config;
  },
  (error) => Promise.reject(error)
);

// 响应拦截器
apiClient.interceptors.response.use(
  (response) => response.data,
  (error) => {
    const message = error.response?.data?.detail || '请求失败';
    return Promise.reject(new Error(message));
  }
);
```

#### API 方法定义（api/stage1.ts）

```typescript
import { apiClient } from './client';

export interface GenerateAttributesRequest {
  concept: string;
  entity_word?: string;
}

export interface AttributeResponse {
  concept: string;
  entity_word: string;
  task_id: string;
  attributes: Attribute[];
  metadata: AttributeMetadata;
}

export const stage1Api = {
  // 生成属性词
  generateAttributes: (data: GenerateAttributesRequest) =>
    apiClient.post<AttributeResponse>('/api/stage1/generate', data),
};
```

### 7.2 API 端点汇总

#### 属性词相关

```typescript
// 生成属性词
POST /api/stage1/generate

// 查询任务详情（包含属性词列表）
GET  /api/stage2/tasks/:taskId

// 更新属性词选择状态
PUT  /api/stage2/tasks/:taskId/selection
```

#### 本体词相关

```typescript
// 生成本体词变体
POST /api/stage3/tasks/:taskId/entity-words/generate

// 查询本体词列表
GET  /api/stage3/tasks/:taskId/entity-words

// 更新本体词选择状态
PUT  /api/stage3/tasks/:taskId/entity-words/selection
```

#### 搜索词相关

```typescript
// 生成搜索词（属性词+本体词组合）
POST /api/stage3/tasks/:taskId/search-terms

// 查询搜索词列表（支持分页）
GET  /api/stage3/tasks/:taskId/search-terms?page=1&page_size=20

// 批量删除搜索词
DELETE /api/stage3/tasks/:taskId/search-terms/batch
```

#### 导出相关

```typescript
// 保存产品信息
POST /api/stage4/save-product-info

// 导出Bulksheet Excel文件
POST /api/stage4/export
```

### 7.3 类型定义（types/）

所有 TypeScript 类型定义应与后端 Pydantic 模型保持一致：

```typescript
// types/attribute.ts
export interface Attribute {
  id: number;
  word: string;
  concept: string;
  type: 'original' | 'synonym' | 'related' | 'variant' | 'custom';
  translation: string;
  use_case: string;
  search_value: 'high' | 'medium' | 'low';
  search_value_stars: number;
  recommended: boolean;
  source: 'ai' | 'user';
  is_selected: boolean;
}

// types/task.ts
export interface Task {
  task_id: string;
  concept: string;
  entity_word: string;
  status: 'draft' | 'selected' | 'entity_selected' | 'combined' | 'exported';
  attributes: Attribute[];
  metadata: TaskMetadata;
  created_at: string;
  updated_at: string;
}
```

---

## 8. 开发计划

### 8.1 开发阶段

#### 阶段 1：项目搭建（1 天）

**目标**: 搭建项目基础架构

**任务**:
- [ ] 初始化 Vite + React + TypeScript 项目
- [ ] 安装依赖（Ant Design, TanStack Query, Zustand, Axios）
- [ ] 配置 ESLint + Prettier
- [ ] 配置 Tailwind CSS
- [ ] 创建基础目录结构
- [ ] 配置路由（React Router）
- [ ] 配置 API 客户端（axios）
- [ ] 配置环境变量（.env）

**产出**:
```
frontend/
├── src/
│   ├── api/
│   │   └── client.ts
│   ├── components/
│   ├── pages/
│   ├── stores/
│   ├── types/
│   ├── utils/
│   ├── App.tsx
│   └── main.tsx
├── .env
├── vite.config.ts
├── tailwind.config.ts
└── package.json
```

---

#### 阶段 2：通用组件开发（2 天）

**目标**: 开发可复用的通用组件

**任务**:
- [ ] Layout 组件（Header, Footer, Breadcrumb）
- [ ] AttributeTable 组件
- [ ] EntityWordTable 组件
- [ ] SearchTermTable 组件
- [ ] StatsCard 组件
- [ ] LoadingOverlay 组件
- [ ] ConfirmModal 组件
- [ ] StageNavigation 组件

**产出**: 8个通用组件，可在所有 Stage 页面复用

---

#### 阶段 3：属性词生成页面开发（1.5 天）

**业务目标**: 完成属性词生成功能，让用户输入概念后AI自动生成属性词

**开发任务**:
- [ ] AttributeGeneratePage 组件开发
- [ ] 输入表单（产品属性概念、产品核心词）
- [ ] 调用后端 API 生成属性词
- [ ] 显示"AI 生成中..."加载状态
- [ ] 显示生成结果（表格 + 统计看板）
- [ ] "进入筛选"按钮
- [ ] 错误处理和提示信息

**测试验证**:
- 输入验证（必填项、格式）
- API 调用成功/失败场景
- 结果数据正确显示

---

#### 阶段 4：属性词筛选页面开发（2 天）

**业务目标**: 完成属性词筛选功能，让用户审核和选择需要的属性词

**开发任务**:
- [ ] AttributeSelectPage 组件开发
- [ ] 从 URL 获取任务ID
- [ ] 调用 API 查询任务详情
- [ ] 属性词表格（勾选、筛选、排序）
- [ ] 添加自定义属性词（弹窗）
- [ ] 删除不需要的词
- [ ] 保存选择（调用 PUT API）
- [ ] "进入本体词扩展"按钮
- [ ] 集成状态管理 (Zustand Store)

**测试验证**:
- 勾选/取消勾选功能
- 快速筛选功能
- 添加自定义词功能
- 删除功能
- 保存成功/失败场景

---

#### 阶段 5：本体词扩展页面开发（2 天）

**业务目标**: 完成本体词扩展功能，让用户生成产品核心词的变体

**开发任务**:
- [ ] EntityWordPage 组件开发
- [ ] "AI 生成本体词变体"按钮
- [ ] 显示生成结果
- [ ] 本体词表格（复用 EntityWordTable 组件）
- [ ] 筛选和编辑功能
- [ ] 保存选择
- [ ] "生成搜索词"按钮
- [ ] 集成状态管理

**测试验证**:
- AI 生成本体词功能
- 表格交互功能
- 保存成功场景

---

#### 阶段 6：搜索词管理页面开发（2 天）

**业务目标**: 完成搜索词管理功能，让用户查看和管理最终的搜索词组合

**开发任务**:
- [ ] SearchTermPage 组件开发
- [ ] 调用 API 查询搜索词（支持分页）
- [ ] 搜索词表格
- [ ] 按来源筛选（属性词、本体词）
- [ ] 批量删除功能
- [ ] 分页控件
- [ ] "导出广告表"按钮

**测试验证**:
- 分页加载功能
- 筛选功能
- 批量删除功能
- 性能测试（大量搜索词数据）

---

#### 阶段 7：广告表导出页面开发（2 天）

**业务目标**: 完成Bulksheet导出功能，让用户输入产品信息和预算后导出Excel文件

**开发任务**:
- [ ] ExportPage 组件开发
- [ ] 产品信息表单（SKU、ASIN、手机型号下拉）
- [ ] 保存产品信息（调用 API）
- [ ] 预算信息表单（每日预算、各项出价）
- [ ] 实时预览信息（广告活动名称、广告组名称）
- [ ] "下载Excel文件"按钮（调用导出 API）
- [ ] 文件下载处理（Blob）
- [ ] 成功提示信息

**测试验证**:
- 表单验证（必填项、格式）
- API 调用
- Excel文件下载成功

---

#### 阶段 8：集成测试与优化（2 天）

**业务目标**: 端到端测试完整业务流程，优化用户体验

**开发任务**:
- [ ] 完整业务流程测试（属性词生成 → 筛选 → 本体词扩展 → 搜索词管理 → 导出）
- [ ] 错误边界处理（Error Boundary）
- [ ] 优化加载状态（骨架屏、进度条）
- [ ] 响应式设计调整（平板、桌面适配）
- [ ] 性能优化（懒加载、代码分割、缓存）
- [ ] 无障碍性优化（键盘导航、屏幕阅读器）
- [ ] 浏览器兼容性测试（Chrome、Safari、Edge）
- [ ] 修复测试中发现的 Bug

---

#### 阶段 9：部署与文档（1 天）

**业务目标**: 部署到生产环境，编写使用文档

**开发任务**:
- [ ] 配置生产环境变量（API地址等）
- [ ] 构建优化（代码压缩、Tree Shaking）
- [ ] 部署到 Vercel/Netlify
- [ ] 编写用户使用手册（如何生成关键词）
- [ ] 编写开发文档（README、技术架构）
- [ ] 整理 API 对接文档

---

### 8.2 时间估算

| 开发阶段 | 天数 | 工作量（人天） |
|------|------|---------------|
| 项目搭建 | 1 | 1 |
| 通用组件 | 2 | 2 |
| 属性词生成页面 | 1.5 | 1.5 |
| 属性词筛选页面 | 2 | 2 |
| 本体词扩展页面 | 2 | 2 |
| 搜索词管理页面 | 2 | 2 |
| 广告表导出页面 | 2 | 2 |
| 集成测试优化 | 2 | 2 |
| 部署与文档 | 1 | 1 |
| **总计** | **15.5天** | **15.5人天** |

**预计工期**: 3-4 周（按每天 8 小时计算）

### 8.3 里程碑

| 里程碑 | 目标日期 | 交付成果 |
|--------|---------|--------|
| M1: 项目初始化 | Day 1 | 可运行的空白项目框架 |
| M2: 通用组件完成 | Day 3 | 6个通用业务组件 |
| M3: 属性词功能完成 | Day 6 | 属性词生成和筛选功能可用 |
| M4: 本体词和搜索词完成 | Day 10 | 完整的搜索词生成流程可用 |
| M5: 导出功能完成 | Day 12 | 完整业务流程可用，可导出Excel |
| M6: 测试完成 | Day 14 | 所有功能测试通过，Bug修复 |
| M7: 正式上线 | Day 15 | 部署到生产环境，可对外使用 |

---

## 9. 部署方案

### 9.1 部署平台选择

#### 推荐方案 1：Vercel（免费 + 自动化）

**优点**:
- ✅ 零配置部署（连接 GitHub 自动部署）
- ✅ 全球 CDN 加速
- ✅ 免费 SSL 证书
- ✅ Preview 部署（PR 预览）
- ✅ 免费额度充足（个人项目够用）

**部署步骤**:
```bash
# 1. 安装 Vercel CLI
npm install -g vercel

# 2. 登录
vercel login

# 3. 部署
vercel
```

#### 推荐方案 2：Netlify

**优点**:
- ✅ 简单易用
- ✅ 支持表单、函数
- ✅ 免费 SSL
- ✅ 持续部署

#### 备选方案：Docker + 自托管

```dockerfile
# Dockerfile
FROM node:18-alpine as builder
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN npm install -g pnpm && pnpm install
COPY . .
RUN pnpm build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/nginx.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### 9.2 环境变量配置

#### 开发环境（.env.development）

```env
VITE_API_BASE_URL=http://localhost:8000
VITE_APP_ENV=development
```

#### 生产环境（.env.production）

```env
VITE_API_BASE_URL=https://your-backend.replit.dev
VITE_APP_ENV=production
```

### 9.3 构建优化

```typescript
// vite.config.ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  build: {
    rollupOptions: {
      output: {
        // 代码分割
        manualChunks: {
          'vendor': ['react', 'react-dom', 'react-router-dom'],
          'antd': ['antd'],
          'query': ['@tanstack/react-query'],
        },
      },
    },
    // 压缩
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true, // 生产环境删除 console
      },
    },
  },
});
```

---

## 10. 附录

### 10.1 目录结构示例

```
frontend/
├── public/
│   ├── favicon.ico
│   └── logo.png
├── src/
│   ├── api/
│   │   ├── client.ts                # axios 配置
│   │   ├── attribute.ts             # 属性词相关API
│   │   ├── entity-word.ts           # 本体词相关API
│   │   ├── search-term.ts           # 搜索词相关API
│   │   └── export.ts                # 导出相关API
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   └── Breadcrumb.tsx
│   │   ├── common/
│   │   │   ├── AttributeTable.tsx
│   │   │   ├── EntityWordTable.tsx
│   │   │   ├── SearchTermTable.tsx
│   │   │   ├── StatsCard.tsx
│   │   │   ├── LoadingOverlay.tsx
│   │   │   └── ConfirmModal.tsx
│   │   └── index.ts
│   ├── hooks/
│   │   ├── useTaskDetail.ts
│   │   ├── useGenerateAttributes.ts
│   │   └── useUpdateSelection.ts
│   ├── pages/
│   │   ├── HomePage.tsx                        # 首页
│   │   ├── AttributeGeneratePage.tsx           # 属性词生成页
│   │   ├── AttributeSelectPage.tsx             # 属性词筛选页
│   │   ├── EntityWordPage.tsx                  # 本体词扩展页
│   │   ├── SearchTermPage.tsx                  # 搜索词管理页
│   │   └── ExportPage.tsx                      # 广告表导出页
│   ├── stores/
│   │   ├── app-store.ts
│   │   ├── stage2-store.ts
│   │   └── stage3-store.ts
│   ├── types/
│   │   ├── attribute.ts
│   │   ├── task.ts
│   │   ├── entity-word.ts
│   │   ├── search-term.ts
│   │   └── index.ts
│   ├── utils/
│   │   ├── format.ts          # 格式化工具
│   │   ├── validation.ts      # 验证工具
│   │   └── download.ts        # 文件下载
│   ├── App.tsx
│   ├── main.tsx
│   └── router.tsx
├── .env
├── .env.production
├── .eslintrc.json
├── .prettierrc
├── index.html
├── package.json
├── pnpm-lock.yaml
├── tailwind.config.ts
├── tsconfig.json
└── vite.config.ts
```

### 10.2 依赖包清单

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "antd": "^5.12.0",
    "@tanstack/react-query": "^5.12.0",
    "zustand": "^4.4.7",
    "axios": "^1.6.2",
    "dayjs": "^1.11.10"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.3.3",
    "vite": "^5.0.8",
    "tailwindcss": "^3.3.6",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "eslint": "^8.55.0",
    "prettier": "^3.1.1"
  }
}
```

### 10.3 快速开始命令

```bash
# 1. 创建项目
pnpm create vite frontend --template react-ts

# 2. 进入目录
cd frontend

# 3. 安装依赖
pnpm install

# 4. 安装核心依赖
pnpm add react-router-dom antd @tanstack/react-query zustand axios dayjs

# 5. 安装 Tailwind CSS
pnpm add -D tailwindcss autoprefixer postcss
npx tailwindcss init -p

# 6. 启动开发服务器
pnpm dev

# 7. 构建生产版本
pnpm build
```

### 10.4 参考资料

- **React 官方文档**: https://react.dev/
- **Ant Design 官方文档**: https://ant.design/
- **TanStack Query 官方文档**: https://tanstack.com/query/latest
- **Zustand 官方文档**: https://docs.pmnd.rs/zustand/getting-started/introduction
- **React Router 官方文档**: https://reactrouter.com/

---

**文档结束**

**编写者**: Claude Code
**最后更新**: 2025-11-07
**版本**: v1.0
