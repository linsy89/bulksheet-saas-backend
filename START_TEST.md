# 🧪 Stage 1 功能测试指南

## 快速开始（3种方式）

---

## 方式 1️⃣: 可视化网页测试 ⭐ 推荐

**最简单、最直观的测试方式**

### 步骤：
1. **打开测试页面**
   ```bash
   open /Users/linshaoyong/Desktop/bulksheet-saas/backend_v2/test_page.html
   ```
   或者直接双击文件 `test_page.html`

2. **测试功能**
   - 页面会自动检测服务器状态
   - 输入任意概念（如 "ocean", "海洋", "cute"）
   - 点击"生成属性词"按钮
   - 等待3-5秒，查看AI生成的结果

3. **预期结果**
   - ✅ 显示绿色状态：服务器运行正常
   - ✅ 生成5-10个属性词
   - ✅ 每个属性词有2-3个变体

---

## 方式 2️⃣: FastAPI自动文档 (Swagger UI)

**专业的API测试界面**

### 步骤：
1. **打开浏览器访问**
   ```
   http://localhost:8002/docs
   ```

2. **测试API**
   - 找到 `POST /api/stage1/generate` 端点
   - 点击 "Try it out"
   - 在Request body输入：
     ```json
     {
       "concept": "ocean"
     }
     ```
   - 点击 "Execute"
   - 查看响应结果

3. **优点**
   - 可以看到完整的API文档
   - 可以看到请求/响应的JSON格式
   - 可以测试所有端点

---

## 方式 3️⃣: 命令行测试 (curl)

**快速验证API**

### 测试健康检查：
```bash
curl http://localhost:8002/health
```

**预期输出：**
```json
{"status":"healthy","message":"API is running"}
```

### 测试Stage 1生成：
```bash
curl -X POST http://localhost:8002/api/stage1/generate \
  -H "Content-Type: application/json" \
  -d '{"concept": "ocean"}' \
  | python3 -m json.tool
```

**预期输出：**
```json
{
  "concept": "ocean",
  "task_id": "uuid-string",
  "candidates": [
    {
      "word": "ocean",
      "variants": ["oceanic", "sea", "marine"]
    },
    {
      "word": "beach",
      "variants": ["coastal", "shore", "seaside"]
    }
    ...
  ]
}
```

### 测试中文输入：
```bash
curl -X POST http://localhost:8002/api/stage1/generate \
  -H "Content-Type: application/json" \
  -d '{"concept": "海洋"}' \
  | python3 -m json.tool
```

---

## ⚙️ 服务器管理

### 检查服务器状态：
```bash
ps aux | grep uvicorn | grep -v grep
```

### 查看服务器日志：
```bash
tail -f /Users/linshaoyong/Desktop/bulksheet-saas/backend_v2/server.log
```

### 重启服务器（如果需要）：
```bash
# 停止
pkill -9 -f "uvicorn"

# 启动
cd /Users/linshaoyong/Desktop/bulksheet-saas/backend_v2
source venv/bin/activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8002
```

---

## 📊 测试案例

### 测试用例 1: 英文概念
**输入:** `ocean`
**预期:** 生成ocean, beach, water, blue, wave等相关属性词

### 测试用例 2: 中文概念
**输入:** `海洋`
**预期:** 翻译成英文后生成ocean相关属性词

### 测试用例 3: 其他概念
**输入:** `cute`, `teen`, `vintage`
**预期:** 每个概念生成5-10个相关属性词

---

## ❓ 常见问题

### Q1: 网页显示"无法连接到服务器"？
**A:** 检查服务器是否运行：
```bash
curl http://localhost:8002/health
```
如果失败，重启服务器（见上方"服务器管理"）

### Q2: API返回500错误？
**A:** 查看服务器日志：
```bash
tail -50 /Users/linshaoyong/Desktop/bulksheet-saas/backend_v2/server.log
```

### Q3: 生成速度很慢？
**A:**
- DeepSeek API调用需要3-5秒
- 如果超过30秒，API会自动切换到备用规则引擎
- 检查网络连接

### Q4: 想要看API的完整文档？
**A:** 访问 http://localhost:8002/docs

---

## ✅ 验证清单

测试完成后，请确认：

- [ ] 服务器健康检查返回正常
- [ ] 能成功生成英文概念的属性词
- [ ] 能成功生成中文概念的属性词
- [ ] 每个属性词都有变体列表
- [ ] 返回了有效的task_id
- [ ] 响应时间在合理范围内（<30秒）

---

## 🎉 测试成功后

如果所有测试都通过，说明：
- ✅ FastAPI服务器运行正常
- ✅ DeepSeek AI集成成功
- ✅ 数据模型验证正确
- ✅ CORS配置正确
- ✅ Stage 1功能完整

**下一步：**
继续实现Stage 2和Stage 3功能！

---

**创建时间:** 2025-11-04
**服务器地址:** http://localhost:8002
**测试页面:** test_page.html
